let currentRun=null,eventSource=null,selectedTasks=null;
let articlePage=1,articleSize=20,articleSource='',articleTotal=0,dataView='pending',selectedArticles=new Set();
const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function json(url,options){const r=await fetch(url,options);let v;try{v=await r.json()}catch{v={detail:await r.text()}}if(!r.ok)throw new Error(typeof v.detail==='string'?v.detail:JSON.stringify(v.detail));return v}
function log(message,time=''){const box=$('logs');box.textContent+=`\n${time?`[${time}] `:''}${message}`;box.scrollTop=box.scrollHeight}
function stats(v){for(const key of ['total','succeeded','skipped','failed'])if(v[key]!=null)$(key).textContent=v[key];const total=v.total||0,done=v.processed||0;$('bar').style.width=`${total?Math.min(100,done/total*100):0}%`}
function watch(id,append=false){currentRun=id;$('runId').textContent=id;$('stop').disabled=false;if(!append)$('logs').textContent='连接实时日志…';if(eventSource)eventSource.close();eventSource=new EventSource(`/api/runs/${id}/events`);eventSource.onmessage=e=>log(e.data);for(const type of ['status','log','warning','error','progress','complete'])eventSource.addEventListener(type,e=>{const v=JSON.parse(e.data);log(v.message,v.time);stats(v);if(type==='complete'||type==='error'){eventSource.close();$('stop').disabled=true;load();if($('panelArticles').style.display!=='none')loadArticles()}})}
function taskLabel(type){return({schedule:'定时抓取',manual:'手动任务',retry:'重试失败','manual-crawl':'手动抓取','manual-push':'入库'}[type]||type)}
async function load(){try{const v=await json('/api/tasks');const checked=selectedTasks===null?new Set(v.tasks.map(t=>t.code)):new Set([...document.querySelectorAll('.task input:checked')].map(x=>x.value));selectedTasks=checked;$('tasks').innerHTML=v.tasks.map(t=>`<label class="task"><input type="checkbox" value="${esc(t.code)}"${checked.has(t.code)?' checked':''}><b>${esc(t.name)}</b><small>${esc(t.rule)}</small></label>`).join('');$('pendingBadge').textContent=v.pending_count?`${v.pending_count} 条待入库`:'';$('runs').innerHTML=v.recent_runs.map(r=>`<tr><td><code>${esc(r.run_id.slice(0,10))}…</code></td><td>${esc(taskLabel(r.trigger_type))}</td><td>${r.dry_run?'预检':'正式'}</td><td>${esc(r.status)}</td><td>${r.succeeded}/${r.skipped}/${r.failed}</td><td>${esc(r.started_at||'-')}</td><td><button class="secondary" onclick="viewRun('${esc(r.run_id)}')">查看</button>${r.failed?` <button class="secondary" onclick="retryRun('${esc(r.run_id)}')">重试</button>`:''}</td></tr>`).join('')}catch(e){log(`加载失败：${e.message}`)}}
async function checkHealth(){try{const v=await json('/api/health');$('health').textContent=v.ok?'服务正常':'配置或依赖异常';$('health').classList.toggle('ok',v.ok)}catch{$('health').textContent='检查失败'}}
async function loadKmsAuthStatus(){try{const v=await json('/api/kms-auth');const names=[v.has_authorization_token?'Authorization-Token':'',v.has_cookie?'Cookie':''].filter(Boolean);$('kmsAuthStatus').textContent=names.length?`已配置：${names.join('、')}`:'可选：普通入库无需配置'}catch{$('kmsAuthStatus').textContent='配置状态读取失败'}}
async function saveKmsAuth(){const authorization_token=$('kmsAuthorizationToken').value.trim(),cookie=$('kmsCookie').value.trim();if(!authorization_token){$('kmsAuthMessage').textContent='请先输入 Authorization-Token';return}try{const v=await json('/api/kms-auth',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({authorization_token,cookie})});$('kmsAuthorizationToken').value='';$('kmsCookie').value='';$('kmsAuthMessage').textContent=v.message;loadKmsAuthStatus()}catch(e){$('kmsAuthMessage').textContent=`保存失败：${e.message}`}}
async function testKmsAuth(){const authorization_token=$('kmsAuthorizationToken').value.trim(),cookie=$('kmsCookie').value.trim();if(!authorization_token){$('kmsAuthMessage').textContent='请先输入 Authorization-Token（普通“保存到知识库”无需此项）';return}if(authorization_token.includes('\\_')){$('kmsAuthMessage').textContent='令牌中含有反斜杠转义。请从浏览器开发者工具直接复制原始 Authorization-Token，不要从聊天文本复制。';return}const button=$('testKmsAuth');button.disabled=true;button.textContent='测试中…';$('kmsAuthMessage').textContent='正在验证正文覆盖更新权限，不会保存或修改 KMS 数据…';try{const v=await json('/api/kms-auth/test',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({authorization_token,cookie})});$('kmsAuthMessage').textContent=v.ok?'鉴权有效，可以保存后执行正文覆盖更新。':`鉴权无效：${v.message}。请确认令牌来自当前爬虫配置的同一 KMS 环境。`}catch(e){$('kmsAuthMessage').textContent=`测试失败：${e.message}`}finally{button.disabled=false;button.textContent='测试正文更新鉴权'}}
function viewQuery(){if(dataView==='pending')return{status:'pending'};if(dataView==='update')return{update_status:'pending'};if(dataView==='updateFailed')return{update_status:'failed'};return{}}
function updateActionControls(){const n=selectedArticles.size,actionable=dataView!=='all',isUpdate=dataView==='update'||dataView==='updateFailed';$('selectionHint').textContent=actionable?(n?`已选择 ${n} 条记录`:`请选择需要${isUpdate?'更新正文':'入库'}的记录`):'“全部记录”仅用于查询；请切换到待入库或待更新后执行操作。';$('previewSelected').disabled=!actionable||n===0;$('executeSelected').disabled=!actionable||n===0;$('checkVisible').disabled=!actionable;$('previewSelected').textContent=isUpdate?'预检匹配':'预检入库';$('executeSelected').textContent=isUpdate?'覆盖更新正文':'保存到知识库'}
function setDataView(next){dataView=next;articlePage=1;selectedArticles.clear();[...$('dataTabs').children].forEach(btn=>btn.classList.toggle('active',btn.dataset.view===next));loadArticles()}
async function loadArticles(){try{const q=viewQuery(),params=new URLSearchParams({page:articlePage,size:articleSize});Object.entries(q).forEach(([k,v])=>params.set(k,v));if(articleSource)params.set('source_code',articleSource);const keyword=$('keyword').value.trim();if(keyword)params.set('keyword',keyword);const [v,c]=await Promise.all([json('/api/articles?'+params),json('/api/articles/counts')]);articleTotal=v.total;$('countPending').textContent=c.pending||0;$('countUpdate').textContent=c.update_pending||0;$('countUpdateFailed').textContent=c.update_failed||0;$('articleStats').textContent=`待入库 ${c.pending||0} · 已同步 ${c.success||0} · 待更新 ${c.update_pending||0} · 更新失败 ${c.update_failed||0} · 无法匹配 ${c.update_unmatched||0}`;$('articlesBody').innerHTML=v.items.map(a=>{const sync=a.kms_status==='success'?'<span class="status success">已同步</span>':a.kms_status==='failed'?`<span class="status failed" title="${esc(a.last_error||a.kms_result_code||'同步失败')}">同步失败</span>`:'<span class="status pending">待入库</span>';const update=a.content_update_status==='success'?'<span class="status success">已更新</span>':a.content_update_status==='failed'?`<span class="status failed" title="${esc(a.content_update_error||'更新失败')}">更新失败</span>`:a.content_update_status==='unmatched'?`<span class="status failed" title="${esc(a.content_update_error||'无法安全匹配 KMS 文档')}">无法匹配</span>`:a.content_update_status==='pending'?'<span class="status pending">待更新</span>':'<span class="muted">无需更新</span>';const fmt=x=>String(x||'').slice(0,16),apply=a.apply_start&&a.apply_end?`${String(a.apply_start).slice(5)} ~ ${String(a.apply_end).slice(5)}`:(fmt(a.apply_start)||fmt(a.apply_end)||'-');return `<tr><td><input type="checkbox" class="row-check" value="${esc(a.policy_crawler_article_id)}"${selectedArticles.has(a.policy_crawler_article_id)?' checked':''}${dataView==='all'?' disabled':''}></td><td class="title" title="${esc(a.title)}">${esc(a.title)}</td><td>${a.source_code==='qifuyun'?'企服云':'随申办'}</td><td>${esc(a.publish_date||'-')}</td><td>${esc(fmt(a.crawled_at))}</td><td>${esc(apply)}</td><td>${sync}</td><td>${update}</td><td>${esc(fmt(a.pushed_at)||'-')}</td></tr>`}).join('')||'<tr><td colspan="9" class="muted">暂无符合条件的数据</td></tr>';const pages=Math.max(1,Math.ceil(v.total/articleSize));$('pageInfo').textContent=`共 ${v.total} 条`;$('pageNo').textContent=`${articlePage} / ${pages}`;$('prevPage').disabled=articlePage<=1;$('nextPage').disabled=articlePage>=pages;$('checkAll').checked=v.items.length>0&&dataView!=='all'&&[...document.querySelectorAll('.row-check')].every(x=>x.checked);updateActionControls()}catch(e){log(`加载数据失败：${e.message}`)}}
async function startCrawl(dryRun){const codes=[...document.querySelectorAll('.task input:checked')].map(x=>x.value);if(!codes.length)return alert('请至少选择一个抓取来源');const refresh_existing=$('refreshExisting').checked,auto_sync=$('autoSync').checked;const message=dryRun?'将获取并检查政策，不写入本地库，也不调用 KMS。确认继续？':refresh_existing?'将重新抓取已有政策并比较原文；有变化时仅进入待更新，不会自动覆盖 KMS。确认继续？':'将抓取政策并保存到本地库。确认继续？';if(!confirm(message))return;try{const v=await json('/api/runs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({task_codes:codes,dry_run:dryRun,confirm_write:!dryRun,phase:'crawl',auto_sync,refresh_existing})});watch(v.run_id)}catch(e){alert(e.message)}}
async function processSelected(dryRun){const ids=[...selectedArticles],isUpdate=dataView==='update'||dataView==='updateFailed';if(!ids.length)return;const action=isUpdate?'覆盖更新 KMS 正文并重新处理':'保存到 KMS 知识库并拆解';const message=dryRun?`将预检 ${ids.length} 条记录，不调用 KMS。确认继续？`:`将${action} ${ids.length} 条记录。确认继续？`;if(!confirm(message))return;try{const v=await json(isUpdate?'/api/articles/update':'/api/articles/push',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({article_ids:ids,dry_run:dryRun,confirm_write:!dryRun})});watch(v.run_id)}catch(e){alert(e.message)}}
function showTab(name){const tasks=name==='tasks';$('panelTasks').style.display=tasks?'':'none';$('panelArticles').style.display=tasks?'none':'';$('tabTasksBtn').classList.toggle('active',tasks);$('tabArticlesBtn').classList.toggle('active',!tasks);if(!tasks)loadArticles()}
$('testKmsAuth').onclick=testKmsAuth;$('saveKmsAuth').onclick=saveKmsAuth;$('previewCrawl').onclick=()=>startCrawl(true);$('startCrawl').onclick=()=>startCrawl(false);$('stop').onclick=async()=>{if(currentRun)try{await json(`/api/runs/${currentRun}/stop`,{method:'POST'})}catch(e){alert(e.message)}};$('previewSelected').onclick=()=>processSelected(true);$('executeSelected').onclick=()=>processSelected(false);$('checkVisible').onclick=()=>{document.querySelectorAll('.row-check:not(:checked)').forEach(x=>{x.checked=true;selectedArticles.add(x.value)});$('checkAll').checked=true;updateActionControls()};$('articlesBody').addEventListener('change',e=>{const box=e.target;if(!box.classList.contains('row-check'))return;box.checked?selectedArticles.add(box.value):selectedArticles.delete(box.value);updateActionControls()});$('checkAll').onchange=e=>{document.querySelectorAll('.row-check').forEach(x=>{x.checked=e.target.checked;e.target.checked?selectedArticles.add(x.value):selectedArticles.delete(x.value)});updateActionControls()};$('dataTabs').onclick=e=>{const button=e.target.closest('.data-tab');if(button)setDataView(button.dataset.view)};$('sourceFilter').onclick=e=>{const button=e.target.closest('.opt');if(!button)return;articleSource=button.dataset.source||'';articlePage=1;[...$('sourceFilter').children].forEach(x=>x.classList.toggle('on',x===button));loadArticles()};$('keyword').onkeydown=e=>{if(e.key==='Enter'){articlePage=1;loadArticles()}};$('refreshArticles').onclick=()=>{articlePage=1;loadArticles()};$('prevPage').onclick=()=>{if(articlePage>1){articlePage--;loadArticles()}};$('nextPage').onclick=()=>{if(articlePage<Math.ceil(articleTotal/articleSize)){articlePage++;loadArticles()}};$('tabTasksBtn').onclick=()=>showTab('tasks');$('tabArticlesBtn').onclick=()=>showTab('articles');
async function retryRun(id){if(!confirm('将仅重推该批次中 KMS 失败的数据，确认继续？'))return;try{const v=await json(`/api/runs/${id}/retry-failed`,{method:'POST'});watch(v.run_id)}catch(e){alert(e.message)}}
async function viewRun(id){try{const run=await json(`/api/runs/${id}`);$('runId').textContent=id;stats(run);const running=run.status==='queued'||run.status==='running';$('stop').disabled=!running;if(running)return watch(id);let text=`批次 ${run.run_id}（${taskLabel(run.trigger_type)} · ${run.dry_run?'预检':'正式'} · ${run.status}）\n总数 ${run.total} · 成功 ${run.succeeded} · 跳过 ${run.skipped} · 失败 ${run.failed}\n开始 ${run.started_at||'-'} · 结束 ${run.finished_at||'-'}\n${run.message||''}\n\n—— 明细（最近 100 条）——`;const items=await json(`/api/runs/${id}/items?page=1&size=100`);items.items.forEach(it=>{text+=`\n[${it.phase}] ${it.status} ${esc(it.article_title||it.source_item_id||'')}${it.message?` ${esc(it.message)}`:''}`});$('logs').textContent=text;$('logs').scrollTop=$('logs').scrollHeight}catch(e){alert(`加载任务失败：${e.message}`)}}
window.viewRun=viewRun;window.retryRun=retryRun;load();checkHealth();loadKmsAuthStatus();setInterval(()=>{load();if($('panelArticles').style.display!=='none')loadArticles()},30000);
